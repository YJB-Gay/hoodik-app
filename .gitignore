# === Project-wide ignores (added to prevent committing build artifacts) ===

# Rust / Cargo build outputs (ignore any directory named `target` anywhere)
target/

# Tauri / tauri build outputs and generated artifacts
src-tauri/target/
src-tauri/gen/
**/src-tauri/target/
**/src-tauri/gen/
.tauri/

# Node / frontend
node_modules/
dist/
dist-ssr/
.vite/

# Android / Gradle build artifacts
.gradle/
**/build/
local.properties

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editors / IDEs
.vscode/
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
build/

# Misc
.env
.env.local
.env.*.local
private_key.pem
Reference/
config.json

# keep Cargo.lock out for libraries; include it for apps if desired
Cargo.lock

# ensure top-level accidental /target entry is covered (older file used leading slash)
/target

# nested/duplicate repo
hoodik-app/hoodik-app

